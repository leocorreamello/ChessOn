namespace ChessApi.Pieces
{
    public class Pawn
    {
        int color; // 1 for white, -1 for black
        int position; // 0-63, where 0 is a1, 1 is b1, ..., 7 is h1, 8 is a2, ..., 63 is h8
    }
}
